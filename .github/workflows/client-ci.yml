name: Client CI

concurrency:
  group: update-image-main
  cancel-in-progress: false

on:
  push:
    paths:
      - "client/**"

jobs:
  build-push-update:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set image tag
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build image
        run: |
          docker build \
          -t ${{ secrets.DOCKERHUB_USERNAME }}/poc09-client:${IMAGE_TAG} \
          ./client

      - name: Push image
        run: |
          docker push \
          ${{ secrets.DOCKERHUB_USERNAME }}/poc09-client:${IMAGE_TAG}

      - name: Update deployment
        run: |
          sed -i "s|image: .*poc09-client:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/poc09-client:${IMAGE_TAG}|" \
          workloads/poc09/client-deployment.yml

      - name: Commit changes
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git pull --rebase origin main
          git add workloads/poc09/client-deployment.yml
          git commit -m "Update client image ${IMAGE_TAG}" || true
          git pull --rebase origin main
          git push