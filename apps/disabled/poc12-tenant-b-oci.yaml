apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: poc12-tenant-b-oci
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: ghcr.io/joseberrocal/charts
      chart: tenant-platform
      targetRevision: 0.1.1
      helm:
        valueFiles:
          - $values/workloads/poc12-gitops/tenant-b-values.yaml

    - repoURL: https://github.com/JoseBerrocal/platform-capstone.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: poc12-tenant-b

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true