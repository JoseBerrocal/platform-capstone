apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.tenant.name }}-default-deny
  namespace: {{ .Values.tenant.name }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
